// SPDX-License-Identifier: GPL-2.0-or-later
import { useEffect, useRef, useState } from 'react';
import { useQuery } from '@tanstack/react-query';
import { ArrowLeft, BookmarkPlus, FolderOpen, RotateCcw, Pencil, Trash2, Scissors, Play } from 'lucide-react';
import type { Bookmark, Details, ExportMode } from '../contracts';
import { api } from '../lib/api';
import { useAction } from '../lib/hooks';
import { duration, filenameError, trimError } from '../lib/format';
import { Button, Empty, Feedback, Modal, PageTitle } from '../components/ui';

export function ReviewView({ id, onBack, onExports, busy }: { id: string | null; onBack: () => void; onExports: () => void; busy: boolean }) {
  const action = useAction(); const video = useRef<HTMLVideoElement>(null); const lastResume = useRef(0); const previewing = useRef(false);
  const [position, setPosition] = useState(0); const [playerError, setPlayerError] = useState<string | null>(null);
  const [start, setStart] = useState(0); const [end, setEnd] = useState(0); const [name, setName] = useState('clip'); const [mode, setMode] = useState<ExportMode>('accurate');
  const [edit, setEdit] = useState<Details | null>(null); const [tagText, setTagText] = useState(''); const [mark, setMark] = useState<Bookmark | null>(null);
  const [remove, setRemove] = useState(false); const [removed, setRemoved] = useState(false);
  const recording = useQuery({ queryKey: ['recording', id], queryFn: () => api.recording(id!), enabled: !!id, refetchInterval: 3000 });
  const item = recording.data;
  const playback = useQuery({ queryKey: ['playback', id, item?.has_playback], queryFn: () => api.playback(id!), enabled: !!id && !!item?.has_playback, staleTime: Infinity });
  const bookmarks = useQuery({ queryKey: ['bookmarks', id], queryFn: () => api.bookmarks(id!), enabled: !!id, refetchInterval: 3000 });
  useEffect(() => { setStart(0); setEnd((item?.duration_ms ?? 0) / 1000); setName(`clip-${id?.slice(0, 8) ?? 'local'}`); setPlayerError(null); setRemoved(false); lastResume.current = 0; }, [id, item?.duration_ms]);
  if (!id) return <Empty title="Choose a recording to review"><p>Open a session from your local library to play, bookmark or trim it.</p><Button onClick={onBack}>Open library</Button></Empty>;
  if (!item) return <><Button onClick={onBack}><ArrowLeft size={17}/>Library</Button><Feedback error={recording.error}/><p role="status">Loading recording…</p></>;
  const clipError = trimError(start, end, item.duration_ms / 1000) || filenameError(name);
  function seek(ms: number) { if (video.current) { previewing.current = false; video.current.currentTime = Math.max(0, ms / 1000); } }
  function persistPosition(ms: number) { if (Number.isFinite(ms) && ms >= 0) void api.resume(id!, ms).catch(e => setPlayerError(`Playback position was not saved: ${String(e)}`)); }
  return <><PageTitle eyebrow="MATCH REVIEW · LOCAL VIDEO" title={item.title} actions={<Button onClick={onBack}><ArrowLeft size={17}/>Library</Button>}>{item.source_label} · {duration(item.duration_ms)} · Original media is never trimmed in place.</PageTitle><Feedback error={recording.error || playback.error || bookmarks.error || action.error || playerError || item.error} message={action.message}/>{removed && <div className="notice row between">Library entry removed; original files and exports were not deleted.<Button onClick={() => void action.run(async () => { await api.restore(id); setRemoved(false); })}>Undo removal</Button></div>}<div className="review-grid"><section><div className="player">{playback.data ? <video ref={video} src={playback.data} controls playsInline preload="metadata" aria-label={`Playback of ${item.title}`} onLoadedMetadata={e => { e.currentTarget.currentTime = Math.min(item.resume_ms / 1000, Math.max(0, e.currentTarget.duration - 0.05)); }} onError={() => setPlayerError('This playback asset could not be decoded. Check Diagnostics and try Recover; the original master remains unchanged.')} onPause={e => persistPosition(e.currentTarget.currentTime * 1000)} onTimeUpdate={e => { const ms = e.currentTarget.currentTime * 1000; setPosition(ms); if (Math.abs(ms - lastResume.current) >= 5000) { lastResume.current = ms; persistPosition(ms); } if (previewing.current && ms >= end * 1000) { previewing.current = false; e.currentTarget.pause(); } }}/> : <div className="player-empty"><Play size={42} aria-hidden="true"/><h2>Playback is not ready</h2><p>Finish the recording first. For interrupted or imported media, Recover prepares a compatible playback copy.</p><Button disabled={busy} busy={action.pending} onClick={() => void action.run(() => api.recover(id), 'Playback preparation completed.')}>Recover / prepare playback</Button></div>}</div><div className="player-tools"><span className="mono">{duration(position)} / {duration(item.duration_ms)}</span><label>Speed<select defaultValue="1" onChange={e => { if (video.current) video.current.playbackRate = Number(e.target.value); }}>{[0.5, 0.75, 1, 1.25, 1.5, 2].map(speed => <option key={speed} value={speed}>{speed}×</option>)}</select></label><Button disabled={!playback.data} onClick={() => { action.clear(); setMark({ id: crypto.randomUUID(), recording_id: id, position_ms: position, label: 'Bookmark', note: '' }); }}><BookmarkPlus size={17}/>Add bookmark</Button></div><section className="panel"><div className="section-title"><h2>Bookmarks & moments</h2><span className="muted">{bookmarks.data?.length ?? 0} saved</span></div>{bookmarks.data?.length === 0 && <p className="muted">Save a moment while playing, or use your global bookmark shortcut during recording.</p>}<div className="bookmark-list">{bookmarks.data?.map(b => <div className="bookmark-row" key={b.id}><button className="bookmark-seek" onClick={() => seek(b.position_ms)}><span className="mono">{duration(b.position_ms)}</span><span><strong>{b.label}</strong>{b.note && <span className="muted">{b.note}</span>}</span></button><Button variant="ghost" aria-label={`Edit bookmark ${b.label}`} onClick={() => { action.clear(); setMark({ ...b }); }}><Pencil size={15}/></Button><Button variant="ghost" busy={action.pending} aria-label={`Delete bookmark ${b.label}`} onClick={() => void action.run(() => api.deleteBookmark(b.id), 'Bookmark deleted.')}><Trash2 size={15}/></Button></div>)}</div><p className="small muted">Manual bookmarks only in this build. League event ingestion and synchronized match automation are not enabled.</p></section><div className="row wrap"><Button onClick={() => void action.run(() => api.reveal(id))}><FolderOpen size={16}/>Show original</Button><Button onClick={() => { action.clear(); setEdit({ title: item.title, favorite: item.favorite, notes: item.notes, tags: item.tags }); setTagText(item.tags.join(', ')); }}><Pencil size={16}/>Title, tags & notes</Button><Button disabled={busy} busy={action.pending} onClick={() => void action.run(() => api.relink(id))}>Relink master</Button><Button disabled={busy} busy={action.pending} onClick={() => void action.run(() => api.recover(id), 'Playback copy verified.')}><RotateCcw size={16}/>Recover</Button><Button disabled={busy || removed} variant="ghost" onClick={() => { action.clear(); setRemove(true); }}>Remove entry</Button></div></section><aside className="panel clip-panel"><div className="eyebrow">NON-DESTRUCTIVE EDIT</div><h2>Make a clip</h2><p className="muted">Set an interval in seconds. Your full recording stays intact.</p><div className="trim-fields"><label>In (seconds)<input aria-label="Trim in seconds" type="number" step="0.001" min="0" value={start} onChange={e => setStart(e.target.valueAsNumber)}/><Button variant="ghost" disabled={!playback.data} onClick={() => setStart(Math.round(position) / 1000)}>Use playhead</Button></label><label>Out (seconds)<input aria-label="Trim out seconds" type="number" step="0.001" min="0" value={end} onChange={e => setEnd(e.target.valueAsNumber)}/><Button variant="ghost" disabled={!playback.data} onClick={() => setEnd(Math.round(position) / 1000)}>Use playhead</Button></label></div><label>Clip name<input value={name} maxLength={100} onChange={e => setName(e.target.value)}/></label><label>Export method<select value={mode} onChange={e => setMode(e.target.value as ExportMode)}><option value="accurate">Accurate · re-encode H.264 / AAC</option><option value="fast">Fast · keyframe-aligned copy</option></select></label><p className="small muted">{mode === 'accurate' ? 'Decodes and re-encodes the selected interval. Audio encoder padding can affect the final duration.' : 'Stream copy may include footage before your selected start. Fast mode is not frame-accurate.'}</p>{clipError && <p className="field-error" role="status">{clipError}</p>}<Button disabled={!playback.data || !!clipError} onClick={() => { if (video.current) { video.current.currentTime = start; previewing.current = true; void video.current.play().catch(e => setPlayerError(String(e))); } }}><Play size={16}/>Preview interval</Button><Button variant="primary" disabled={!!clipError || !item.has_playback} busy={action.pending} onClick={() => void action.run(async () => { await api.queue({ recording_id: id, start_ms: start * 1000, end_ms: end * 1000, name, mode }); onExports(); })}><Scissors size={17}/>Queue export</Button><p className="small muted">Exports are saved in a managed local exports folder with unique names. Encoding pauses during capture and restarts safely afterward.</p></aside></div><Modal open={!!edit} onClose={() => { if (!action.pending) setEdit(null); }} title="Recording details" description="These changes update your local library and manifest, not the encoded video.">{edit && <form onSubmit={e => { e.preventDefault(); void action.run(async () => { await api.edit(id, { ...edit, tags: tagText.split(',').map(s => s.trim()).filter(Boolean) }); setEdit(null); }); }}><label>Title<input required maxLength={180} value={edit.title} onChange={e => setEdit({ ...edit, title: e.target.value })}/></label><label>Tags (comma-separated)<input value={tagText} onChange={e => setTagText(e.target.value)}/></label><label>Notes<textarea maxLength={10000} rows={5} value={edit.notes} onChange={e => setEdit({ ...edit, notes: e.target.value })}/></label><label className="check"><input type="checkbox" checked={edit.favorite} onChange={e => setEdit({ ...edit, favorite: e.target.checked })}/>Favorite</label><Feedback error={action.error}/><Button type="submit" variant="primary" busy={action.pending}>Save details</Button></form>}</Modal><Modal open={!!mark} onClose={() => { if (!action.pending) setMark(null); }} title="Bookmark" description="Save an ordinary playback position. This is not a frame-accurate marker.">{mark && <form onSubmit={e => { e.preventDefault(); void action.run(async () => { await api.saveBookmark(mark); setMark(null); }); }}><label>Label<input required maxLength={120} value={mark.label} onChange={e => setMark({ ...mark, label: e.target.value })}/></label><label>Position (seconds)<input type="number" step="0.001" min={0} max={item.duration_ms / 1000} required value={mark.position_ms / 1000} onChange={e => setMark({ ...mark, position_ms: e.target.valueAsNumber * 1000 })}/></label><label>Note<textarea rows={3} maxLength={2000} value={mark.note} onChange={e => setMark({ ...mark, note: e.target.value })}/></label><Feedback error={action.error}/><Button type="submit" variant="primary" busy={action.pending}>Save bookmark</Button></form>}</Modal><Modal open={remove} onClose={() => { if (!action.pending) setRemove(false); }} title="Remove library entry?" description="This hides the entry only. The original master, playback copy and any dependent exported clips stay on disk. No video files will be deleted."><Feedback error={action.error}/><div className="row"><Button onClick={() => setRemove(false)}>Keep entry</Button><Button variant="danger" busy={action.pending} onClick={() => void action.run(async () => { await api.remove(id); setRemoved(true); setRemove(false); })}>Remove entry only</Button></div></Modal></>;
}
